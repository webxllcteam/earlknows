# Railway Postgres exposes this natively — reference it as ${{Postgres.DATABASE_URL}}
DATABASE_URL=postgres://user:pass@host:5432/railway

# openssl rand -base64 32   — never change once there is data
PAYLOAD_SECRET=

# Public origin, used for admin links and canonical URLs
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
